spring.application.name=chessmaster-pro-backend

server.port=${SERVER_PORT:8081}

# Supabase transaction-pooler defaults.
# Keep DB_PASSWORD outside source control and provide it via environment variable.
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
spring.datasource.username=${DB_USERNAME:postgres.tckcfpscxwzmetudpikc}
spring.datasource.password=${DB_PASSWORD:@Abhisupabase}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.sql.init.mode=always
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:2}
spring.datasource.hikari.data-source-properties.sslmode=require
spring.datasource.hikari.data-source-properties.prepareThreshold=0

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

jwt.secret=${JWT_SECRET:ZmFrZS1sb2NhbC1kZXZlbG9wbWVudC1zZWNyZXQtdGhhdC1zaG91bGQtYmUtcmVwbGFjZWQ=}
jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}

bot.stockfish.path=${BOT_STOCKFISH_PATH:stockfish}
bot.stockfish.command-timeout-ms=${BOT_STOCKFISH_TIMEOUT_MS:5000}

fide.import.combined-source=${FIDE_COMBINED_SOURCE:https://ratings.fide.com/download/players_list_xml.zip}
fide.import.standard-source=${FIDE_STANDARD_SOURCE:}
fide.import.rapid-source=${FIDE_RAPID_SOURCE:}
fide.import.blitz-source=${FIDE_BLITZ_SOURCE:}
fide.import.sync-on-startup=${FIDE_SYNC_ON_STARTUP:true}
fide.import.refresh-enabled=${FIDE_REFRESH_ENABLED:true}
fide.import.refresh-cron=${FIDE_REFRESH_CRON:0 0 6 10 * *}
